#include <iostream> 
using namespace std; 
void timphong(int tang, int phong) {
    if (phong > 2) {
        int tangtren = tang + 1;
        int phongtren = phong - 2;
        if (tangtren > n) cout << "-1";
        int ngangphai = tang;
        int phongphai = phong - 2;
        if (phongphai < sophongtanghientai*2 ) { 
                cout << "-1"
            }
        int ngangtrai = tang; 
        int phongtrai = phong + 2;
        if (phongtrai > sophongtanghientai*2 ) {
        cout << "-1";}
    }
    if (phong == 2) {
        int tangtren = -1;
    }

    int tangduoi = tang - 1 
    int phongduoi = phong + 2;

}
int main () {
    int sotang_n;
    int tanghientai;
    int phonghientai;
    cin >> sotang_n >> tanghientai >> phonghientai;
    int sophongtangduoicung = n + 2;
    int sophongtanghientai = sophongtangduoicung - tanghientai; 
}