#include <iostream>
#include <vector>
using namespace std;

int findLargestNumber(const vector<int>& array) {
    // Initialize largestNumber to the first element of the array
    
    // For each element in the array
        // If the element is greater than largestNumber
            // Update largestNumber to the element

    // Return largestNumber
}

int main() {
    vector<int> numbers = {3, 5, 7, 2, 8, 6};
    cout << "The largest number is: " << findLargestNumber(numbers) << endl;
    return 0;
}